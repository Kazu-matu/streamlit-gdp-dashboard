# AI × Python 製造業分析フレームワーク

**EUC向け 活用ガイド v3**

> 「知りたいこと」を言葉にするだけで、分析・最適化・品質管理が手元に完成する

| 項目 | 内容 |
|---|---|
| 対象 | 製造業 EUC・研究・技術部門 |
| 前提知識 | Excel操作ができれば十分 |
| 作成者 | 情シ K.Matsushima |
| 作成日 | 2026年05月21日 |

---

## 目次

1. [データ準備 — 入口はExcelでOK](#1-データ準備--入口はexcelでok)
2. [実験データ分析](#2-実験データ分析)
3. [実験計画法](#3-実験計画法)
4. [品質管理](#4-品質管理)
5. [数理最適化](#5-数理最適化)
6. [機械学習](#6-機械学習)
7. [可視化ライブラリ](#7-可視化ライブラリ)
8. [フレームワーク全体像](#8-フレームワーク全体像)
9. [AIへの指示テンプレート集](#9-aiへの指示テンプレート集)
10. [セキュリティ前提条件](#10-セキュリティ前提条件)

---

## 1. データ準備 — 入口はExcelでOK

「うちはDBがない」は理由になりません。手元のExcelファイルや、機器から出力されるCSVファイルがあれば、今日からデータ分析を始められます。

データの準備はAIがサポートします。「このExcelを読み込んで、欠損値を確認して前処理して」と伝えるだけで、AIがpandasのコードを生成します。

| データソース | Pythonでの読み込み方 | 活用例 |
|---|---|---|
| 🗄️ Oracle / PostgreSQL | `oracledb` / `SQLAlchemy` で接続 | 生産管理DB・販売管理DBの基幹データ活用 |
| 📊 Excel / CSV | `pd.read_excel()` / `pd.read_csv()` | 実験データ・検査記録・生産実績（今日から） |
| 🌐 Web API / IoT | `requests` ライブラリで取得 | 気象データ・IoTセンサーのリアルタイム値 |
| 📄 PDF / テキスト | `pdfplumber` / `pypdf` で抽出 | 検査報告書・仕様書のテキスト化・構造化 |

> **AIへの伝え方（例）**
> 「この実験データのExcelを読み込んで、処理条件ごとに品質指標を比較するグラフを作って」
> → AIが `read_excel`・`groupby`・グラフのコードを一気に生成します。

---

## 2. 実験データ分析

「この処理条件が品質にどう影響するか」をPythonで即可視化。繰り返し実験の傾向を自動で整理します。

| 分析手法 | 内容・活用場面 | ライブラリ |
|---|---|---|
| 処理条件ごとの品質比較 | 温度・圧力・時間などをX軸、品質指標をY軸にした散布図マトリクス。条件の組み合わせ効果が一目でわかる。 | pandas / Plotly |
| バッチ間のばらつき分析 | 箱ひげ図・ヒートマップでロット間差異を可視化。外れ値バッチの条件を自動フラグ。 | plotnine / seaborn |
| 相関・多変量解析 | 原料物性・プロセス条件・製品品質の相関行列を自動生成。隠れた関係性をデータから発見。 | scipy.stats / pandas |

**AIへの指示イメージ**

```
YOU : 実験結果のCSV、処理温度と反応時間が品質スコアに与える影響を
      ヒートマップで見せて

 AI : pivot_table で集計 → Plotlyのimshowでインタラクティブ
      ヒートマップを生成します。
```

---

## 3. 実験計画法

「何をどの順序で実験すべきか」をAIとPythonが設計。実験コストを最小化しながら最適条件を探索します。

| 手法 | 特徴・適した場面 | ライブラリ |
|---|---|---|
| 直交表・田口メソッド | 多因子実験を最小回数で設計。主効果・交互作用を分析。パルプ蒸解条件最適化に実績。 | pyDOE2 |
| 応答曲面法（RSM） | 中心複合計画・Box-Behnken計画で非線形な最適点を探索。3因子で最高品質になる組み合わせを特定。 | statsmodels |
| ベイズ最適化 | 実験結果を学習しながら次の実験点を自動提案。試行回数が少なくても最適解に収束。 | optuna |

### 具体例：パルプ製造の蒸解条件最適化

- **因子**：蒸解温度（160〜180℃）、薬液濃度（14〜18%）、蒸解時間（60〜90分）
- **目的**：カッパー価を目標範囲に保ちながら収率を最大化
- **結果**：直交表L9で9回の実験で最適条件を特定。従来の一因子ずつ変化させる方法より実験回数を **70%削減**。

---

## 4. 品質管理

手書き・手計算だった品質管理をPythonで自動化。最新データが入ったら管理図が自動更新されます。

| 管理手法 | 内容・自動化できること | AIへの指示・具体例 |
|---|---|---|
| シューハート管理図（X̄-R図・X̄-S図） | CSVを読み込んで管理限界線（UCL・LCL）を自動計算・描画。ネルソン則違反を自動検出してアラート表示。 | 「品質データのCSVでX̄-R管理図を作って。ルール違反の点は赤くして」→ 管理限界を自動計算、Plotlyで生成。違反点を赤マーク。 |
| 工程能力指数（Cp・Cpk） | 規格上下限と実測データからCp・Cpkを自動算出。ヒストグラム＋正規分布曲線で工程の実力を一目で把握。 | 製紙工場の坪量管理：毎時計測CSVを読み込み → X̄-R図を自動更新 → 管理外れをTeamsに自動通知。 |
| CUSUM・EWMA管理図 | 微小な工程シフトを早期検知。シューハート管理図では見逃しやすいトレンドをリアルタイムで監視。 | 品質異常の発見が平均 **2時間** 早まった実績。 |

管理図はStreamlitで社内公開すると、品質部門が毎朝URLを開くだけで最新の工程状況を確認できます。更新はデータを差し替えるだけで自動反映されます。

---

## 5. 数理最適化

「コスト最小」「生産量最大」「納期厳守」など複数の条件が絡み合う問題を数式として解きます。

> **メタファー：囲碁の「最善手」を瞬時に計算する**
> ベテランの調達担当が「原料Aをどこからどれだけ買うか」を経験で決めているとき、その判断の裏には複数の制約（コスト・納期・品質・在庫）が絡み合っています。数理最適化はその全パターンをコンピュータが計算し、「この配分が数学的に最善」という答えを出します。

| 手法 | 特徴 | ライブラリ |
|---|---|---|
| 線形計画法（LP） | 目的関数・制約条件が線形の問題。原料配合・生産量配分・コスト最小化。最も実績が多い手法。 | PuLP / scipy |
| 混合整数計画法（MIP） | 「整数でなければならない」制約を扱う。スケジューリング・設備選定・配送ルート。 | OR-Tools |
| メタヒューリスティクス | 遺伝的アルゴリズム・焼きなまし法。厳密解が難しい大規模問題に適用。 | DEAP / optuna |

### 製造業での最適化事例 3選

| 課題・分野 | 決定変数 | 目的関数 | 主な制約 | ライブラリ |
|---|---|---|---|---|
| 原料配合最適化 | 原料A〜Fの配合比率 | 原料コスト最小化 | 白色度≥85、強度≥指定値 | PuLP / CVXPY |
| 配送ルート最適化（VRP） | 各車両の訪問順序 | 総走行距離最小化 | 積載量上限、配送時間窓 | OR-Tools / networkx |
| 生産スケジューリング | 製品×設備の割り付け | 段取り時間最小化 | 納期、設備稼働時間 | OR-Tools CP-SAT |

**AIへの指示例**

```
[原料配合最適化]
YOU : 原料A〜Fのコストと品質特性がこのExcelにある。白色度85以上・
      強度指定値以上の制約でコスト最小の配合比を出して
 AI : PuLP で線形計画問題を定式化して解きます。最適配合比と
      達成コストを表とグラフで表示。

[生産スケジューリング最適化]
YOU : 受注一覧のExcelと設備の段取り時間表がある。納期遵守率を
      最大にする生産順序を計算して、ガントチャートで見せて
 AI : OR-Tools CP-SATソルバーでスケジューリング問題を定式化。
      PlotlyのGanttチャートで結果を可視化。
```

最適化の結果はExcelやCSVにエクスポートして既存の業務フローに組み込めます。PuLPで解いた原料配合比をExcelに書き出し、発注担当者がそのまま使う運用が典型例です。

---

## 6. 機械学習

「データから規則を自動で見つける」— 分類・予測・クラスタリングの3種で製造現場の課題を解きます。

| カテゴリ | サブタイトル | 製造業での活用例 | アルゴリズム |
|---|---|---|---|
| ① 分類 | 「これは何か」を自動判定 | 外観検査の良品/不良品自動判定、設備状態の正常/要注意/要停機分類、不採算品目の自動ラベリング | 決定木 / ランダムフォレスト / SVM |
| ② 予測（回帰） | 「次に何が起きるか」を数値化 | 需要予測・在庫最適化、設備余命予測（RUL）、営業利益の期末着地予測 | Prophet / XGBoost / SARIMA |
| ③ クラスタリング | 「隠れたグループ」を自動発見 | 顧客・品目のセグメンテーション、工程異常パターンの自動検出、類似バッチの自動グルーピング | K-Means / DBSCAN / 階層型 |

機械学習モデルの精度向上には大量のデータと試行錯誤が必要ですが、AIがコードを生成するため「まず動かしてみる」ハードルが大幅に下がりました。まずMVCで試して、価値が確認できたらデータを増やす進め方が現実的です。

---

## 7. 可視化ライブラリ

「どう見せたいか」を伝えれば、AIが最適なライブラリのコードを生成します。

| ライブラリ | タイプ | 特徴 | 代表的な用途 |
|---|---|---|---|
| Plotly | インタラクティブ | ホバー・ズーム・フィルタ標準装備。経営報告・KPIダッシュボード・管理図に最適。 | X̄-R管理図 / 散布図マトリクス / Ganttチャート |
| Altair | 宣言的・シンプル | 「何を見せたいか」を短いコードで記述。パレート分析・散布図マトリクスに強い。 | パレート / ポートフォリオ / ファセット |
| plotnine | 統計可視化 | Rのggplot2と同じ文法。統計的な傾向線・信頼区間・分布の可視化に強い。 | 箱ひげ図 / 回帰線 / 信頼区間 |
| folium | 地図表示 | 配送ルート・工場立地・物流拠点を地図上に可視化。Streamlitと組み合わせ可。 | 配送ルート / 拠点マップ |

可視化はStreamlit・stlite HTMLで社内公開すると、データを見る側がPythonを知らなくても最新のグラフをブラウザで確認できます。情報共有のボトルネックを解消します。

---

## 8. フレームワーク全体像

このフレームワークは「データソース（入口）→ 分析・可視化（中身）→ ML・最適化（応用）→ 出口（共有）」の4層で構成されています。

```
┌─────────────────┐   ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│ ① データソース    │   │ ② 分析・可視化     │   │ ③ ML・最適化       │   │ ④ 出口（共有）     │
│   （入口）        │ → │   （中身）         │ → │   （応用）         │ → │                   │
├─────────────────┤   ├──────────────────┤   ├──────────────────┤   ├──────────────────┤
│ Oracle/PostgreSQL│   │ 実験データ分析       │   │ 実験計画法          │   │ Streamlit 社内公開 │
│ Excel / CSV      │   │ 品質管理・管理図      │   │ ベイズ最適化         │   │ stlite HTML       │
│ Web API / IoT    │   │ 可視化（Plotly等）   │   │ 分類・予測・クラスタ  │   │ CSV/Excel エクスポート│
│ PDF / テキスト    │   │ 損益分析・パレート     │   │ 原料配合最適化(PuLP) │   │ Teams/メール 自動通知 │
└─────────────────┘   └──────────────────┘   └──────────────────┘   └──────────────────┘
```

### まとめ — 「問いを持つ人」が最強のデータアナリストになる

| ポイント | 内容 |
|---|---|
| 言葉がコードになる | 「こう分析したい」をAIに伝えれば、データ加工・グラフ・ML・最適化が全部動き出す。 |
| Excelから始められる | DBがなくてもCSV・Excelで今日から開始。実験データ・検査記録がそのまま分析できる。 |
| 目で見て即判断 | 管理図・実験結果・最適化計画がインタラクティブグラフで可視化。数字の羅列を卒業。 |
| 社内公開が爆速 | Streamlitで完成したらURLを送るだけ。バージョン管理・ファイル配布が不要に。 |

---

## 9. AIへの指示テンプレート集

「課題カテゴリ」と「手元のデータ形式」をAIに伝えるだけで、完動するPythonコードが生成されます。下記のテンプレートをそのままコピーして使ってください。

| 課題カテゴリ | AIへの指示テンプレート | 生成されるコード・出力 |
|---|---|---|
| 実験データ分析 | 処理温度と反応時間が品質スコアに与える影響をヒートマップで見せて | `pivot_table` → Plotly `imshow` でインタラクティブヒートマップ |
| 品質管理図 | 品質データのCSVでX̄-R管理図を作って。ルール違反の点は赤くして | 管理限界自動計算 → Plotly管理図 → ネルソン則違反を赤マーク |
| 原料配合最適化 | 白色度85以上・強度≥指定値の制約でコスト最小の配合比を出して | PuLP定式化 → 最適配合比と達成コストを表とグラフで表示 |
| 需要予測 | 過去3年の月次売上CSVから来月・再来月をProphetで予測して | Prophet学習 → 予測値・95%信頼区間をmatplotlibで可視化 |
| 生産スケジュール | 納期遵守率を最大にする生産順序を計算してGanttチャートで見せて | OR-Tools CP-SAT → Plotly Ganttチャートで結果可視化 |
| 異常検知 | 設備センサーデータで正常データのみから外れ値を検出して | Isolation Forest / DBSCAN → 散布図で異常点をマーク |

---

## 10. セキュリティ前提条件

利用前に必ず確認してください。

- **pip-audit でライブラリの脆弱性チェック** — 使用前に `pip-audit` を実行して `No vulnerabilities` を確認
- **機密情報を含めない** — 個人情報・未公開営業情報・社外秘技術はコードやデータに含めない
- **認証情報をコードに直書きしない** — パスワード・APIキーは `os.environ.get()` で環境変数から取得
- **成果物は社内に留める** — GitHub Publicリポジトリへのpush・社外クラウドへのアップロード禁止

---

<p align="center"><strong>「知りたいこと」を言葉にするだけで分析・最適化・品質管理が手元に完成する</strong></p>
<p align="center"><em>コードを書く力より、「何を知りたいか・何を最適化したいか」を言語化する力が重要です。</em></p>

---

*作成日：2026年05月21日　作成者：情シ K.Matsushima*
