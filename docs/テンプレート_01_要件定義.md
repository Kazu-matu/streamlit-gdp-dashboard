# 要件定義書

| 項目 | 内容 |
| --- | --- |
| 作成日 | YYYY-MM-DD |
| 最終更新日 | YYYY-MM-DD |
| 版数 | v1.0 |
| 作成者 | （名前） |
| 承認者 | （名前） |
| 承認日 | YYYY-MM-DD |

---

## 1. プロジェクト概要

### 1-1. 背景・目的

（このシステムを作る理由・解決したい課題を記載）

### 1-2. スコープ

**対象とすること:**

- （例: 社内ユーザーによるデータの登録・更新・削除）
- （例: 管理者による一覧閲覧・CSV 出力）

**対象としないこと（除外事項）:**

- （例: 外部システムとの連携）
- （例: スマートフォンアプリ対応）

---

## 2. 利用者

| ロール | 説明 | 主な操作 |
| --- | --- | --- |
| 一般ユーザー | （例: 営業担当者） | データの登録・編集・削除 |
| 管理者 | （例: チームリーダー） | 全データの閲覧・集計 |

---

## 3. 業務フロー（現状 → 改善後）

### 現状の業務フロー

```text
（現在の手作業・Excel 管理などのフローを記載）

例:
担当者 → Excel に手入力 → メール添付で上長へ送付 → 上長が確認印
```

### 改善後の業務フロー

```text
（新システム導入後のフローを記載）

例:
担当者 → Web画面でデータ入力 → 自動保存・履歴記録 → 上長がブラウザで確認
```

---

## 4. 機能要件

### 4-1. 主要機能一覧

| 機能ID | 機能名 | 概要 | 優先度 |
| --- | --- | --- | --- |
| F-01 | データ登録 | フォームから新規データを登録する | 必須 |
| F-02 | データ一覧表示 | 登録データを一覧で表示する | 必須 |
| F-03 | データ編集 | 登録済みデータを修正する | 必須 |
| F-04 | データ削除 | 登録済みデータを論理削除する | 必須 |
| F-05 | 変更履歴表示 | データの変更履歴を確認する | 推奨 |
| F-06 | （追加機能） | （説明） | 任意 |

### 4-2. 機能詳細

#### F-01. データ登録

- **入力項目:**
  - （例: タイトル）: 必須、最大 XX 文字
  - （例: 内容）: 任意、最大 XX 文字
  - （例: カテゴリ）: 選択式、〇〇・〇〇・〇〇
- **処理内容:**
  - バリデーション後に DB に保存する
  - 登録者・登録日時を自動記録する
  - 変更履歴（CREATE）を自動記録する
- **完了後の動作:**
  - 一覧画面にリダイレクト（PRG パターン）
  - 「登録しました」メッセージを表示

#### F-02. データ一覧表示

- **表示項目:**
  - ID、タイトル、（その他項目）、登録日時、操作ボタン
- **ソート:**
  - デフォルト: 登録日時の降順
- **フィルタ:**
  - （例: 未完了のみ表示）
  - （例: キーワード検索）

#### F-03. データ編集

- **遷移:** 一覧の「編集」ボタン → 編集フォーム
- **処理:** バリデーション後に DB を更新、変更履歴（UPDATE）を記録
- **完了後:** 一覧画面へリダイレクト

#### F-04. データ削除

- **方式:** 論理削除（`is_deleted = True`）。DB からは消えない
- **確認:** 削除前に確認ダイアログを表示する
- **処理:** 変更履歴（DELETE）を記録
- **完了後:** 一覧画面へリダイレクト

---

## 5. 非機能要件

| 区分 | 要件 | 基準値 |
| --- | --- | --- |
| 性能 | ページ表示速度 | 通常操作で 2 秒以内 |
| 可用性 | 稼働時間 | 平日 8:00〜20:00（情報部門のサーバー稼働時間に準ずる） |
| 同時接続 | 最大同時ユーザー数 | （例: 10 名） |
| データ保持 | データ保存期間 | （例: 3 年間）|
| バックアップ | 頻度・方式 | 情報部門の標準バックアップポリシーに準ずる |
| セキュリティ | アクセス制御 | 社内ネットワークからのみアクセス可 |
| ブラウザ | 動作保証対象 | Google Chrome 最新版、Microsoft Edge 最新版 |

---

## 6. 画面一覧

| 画面ID | 画面名 | URL | 説明 |
| --- | --- | --- | --- |
| SCR-01 | 一覧・登録画面 | `/` | データ一覧と新規登録フォーム |
| SCR-02 | 編集画面 | `/{id}/edit` | 既存データの更新フォーム |
| SCR-03 | （追加画面） | （URL） | （説明） |

---

## 7. データ要件

### 7-1. 主要データ項目

| 項目名 | 型 | 必須 | 最大長 | 説明 |
| --- | --- | --- | --- | --- |
| タイトル | 文字列 | 必須 | 100文字 | （説明） |
| 内容 | 文字列 | 任意 | 2000文字 | （説明） |
| （追加項目） | （型） | （必須/任意） | （長さ） | （説明） |

### 7-2. データ保持ルール

- 削除データは論理削除で保持する（物理削除しない）
- 全変更操作の履歴を保持する
- 登録者・更新者・日時を自動記録する

---

## 8. 外部システム連携

| 連携先 | 連携方向 | 内容 | 時期 |
| --- | --- | --- | --- |
| （例: Active Directory） | 参照 | ログインユーザー名の取得 | 初回リリース |
| （例: 既存システム） | 参照/連携 | （説明） | v2.0 以降 |

（連携なしの場合は「なし」と記載）

---

## 9. 制約・前提条件

- 開発言語: Python 3.12 以上
- Web フレームワーク: FastAPI + Jinja2（SSR）
- DB: 開発環境は SQLite、本番は PostgreSQL（情報部門が管理）
- パッケージ管理: uv
- 本番サーバーは情報部門が用意・管理する
- 認証機能は今回のスコープ外（社内 VPN 内限定アクセスで代替）

---

## 10. 変更履歴

| 版数 | 変更日 | 変更者 | 変更内容 |
| --- | --- | --- | --- |
| v1.0 | YYYY-MM-DD | （名前） | 初版作成 |
